name: eventure
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Genellikle bu şekilde bir aralık belirtmek daha iyidir

dependencies:
  flutter:
    sdk: flutter

  # --- DİĞER TÜM PAKETLER BURAYA, HER BİRİ KENDİ SATIRINDA OLMALI ---
  cupertino_icons: ^1.0.8
  http: ^1.1.0  # Sadece en güncel olanı tutuyoruz
  image_picker: ^1.0.4
  intl: ^0.18.1
  shared_preferences: ^2.0.15


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0 # Sürümü güncelleyebilirsiniz

flutter:
  uses-material-design: true
  assets:
    - assets/